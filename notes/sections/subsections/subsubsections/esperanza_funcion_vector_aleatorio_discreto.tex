\documentclass[../../../main.tex]{subfiles}

\begin{definition}
    El valor esperado de una función  $h(X, Y)$ esta dado por
    \begin{equation*}
        E(h(X, Y)) = \sum_{x}\sum_{y} h(x, y) \cdot p_{X, Y}(x, y)
    \end{equation*}
\end{definition}